## Packages
recharts | Dashboard analytics charts and portfolio visualization
date-fns | Human-readable date formatting for trades and jobs
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
- App is locked to dark theme as requested (no blue, magenta/purple primary).
- SSE endpoint is used for real-time invalidation of caches.
- Assumes shadcn components are present in `@/components/ui/`.
- Using Outfit for headings and JetBrains Mono for financial figures.
